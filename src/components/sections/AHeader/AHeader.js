import React, { Component } from 'react';

import './AHeader.css';

export default class AHeader extends Component {
    render () {
        return (
            <header>
                <h1>React Blog</h1>
            </header>
        )
    }
}